/* fmt/printf.h — included by trace.h when HAVE_FMT_PRINTF is set.
 * We set HAVE_FMT_PRINTF=0 in our config.h, so this is rarely included,
 * but provide it anyway for completeness. */
#ifndef FMT_PRINTF_H_
#define FMT_PRINTF_H_
#include "format.h"
#endif
